# AI TRPG Game Master Project - Master Instructions

## 1. 프로젝트 개요 및 아키텍처
- **프로젝트 명:** AI TRPG Game Master (현대판 대한민국 배경)
- **목적:** LangGraph, FastAPI, Next.js, Supabase를 활용한 멀티 에이전트 텍스트 RPG 웹/앱 서비스 구축
- **핵심 기술 스택:**
  - Frontend: Next.js 16 (App Router), TypeScript, Tailwind CSS + Shadcn UI, Vercel AI SDK (※ 게임 행동 스트림에는 쓰지 않음 — 자체 SSE 규격, `docs/01` 8장)
  - Backend: Python 3.10+, FastAPI, LangChain & LangGraph, Pydantic v2
  - DB & Auth: Supabase (PostgreSQL), Supabase Auth, pgvector
  - Infra/DevOps: Vercel, Render/Railway, Git/GitHub, LangSmith

## 2. 소통 및 설명 원칙 (비전공자 눈높이)
- 나는 비전공자 개발자이다. 복잡한 기술 용어나 코드를 다룰 때는 **일상적인 비유를 들어 아주 쉽게 설명**해 달라.
- 코드를 수정하거나 작성할 때는 **"왜 이 코드가 필요한지", "어떤 원리로 동작하는지"** 핵심 의미를 친절하게 설명해 달라.
- 답변은 불필요한 인사말이나 반복을 줄이고, **변경되는 핵심 코드 스니펫(Snippet) 위주로 간결하게 구성**하여 토큰을 효율적으로 사용해 달라.

## 3. 코딩 컨벤션 및 언어 규칙
- **README.md 작성:** 모든 README.md 파일은 **반드시 한글로만 작성**한다. (영어 사용 금지)
- **코드 주석 및 Docstring:** 프론트엔드와 백엔드 구분 없이, **모든 코드 내부의 주석과 Docstring은 전부 한글로 작성**한다. (사용자가 비전공자이므로 직관적으로 이해할 수 있어야 함)
- **코드 자체의 네이밍:** 변수명, 함수명, 클래스명 등 코드 식별자는 기존 개발 관례에 따라 영어로 작성하되, 설명하는 텍스트는 무조건 한글을 사용한다.

## 4. 디버깅 워크플로우
- 코드가 동작하다 오류가 발생했을 때, 오류 내용(에러 메시지, 스택 트레이스), 로그, 재현 상황을 바탕으로 아래 형식에 맞춰 분석과 수정 방안을 제시한다.
- **오류 분석 및 답변 형식:**
  1. **오류 요약:** 무엇이 왜 잘못됐는지 (비유 포함, 비전공자 눈높이)
  2. **원인 분석:** 로그/코드 기반 근본 원인
  3. **수정 방안:** 구체적인 코드 수정 스니펫 (한글 주석 포함)
  4. **동작 확인 방법:** 수정 후 어떻게 테스트하는지
  5. **커밋 메시지 제안:** Conventional Commits 규칙 준수 (예: `fix: ...`)

## 5. 단계별 작업 및 개발 로그 / 티스토리 블로그 초안 관리 규칙
- 작업을 한 번에 거대하게 처리하지 말고, **아주 작은 단계(Step-by-Step)**로 나누어 진행해 달라.

- **산출물은 두 종류이고, 작성 주체가 다르다. (혼동 금지)**

  | 산출물 | 누가 쓰나 | 언제 |
  |---|---|---|
  | **개발 로그(.txt)** | 프론트엔드 / 백엔드 / 아키텍처 등 **모든 작업 세션 필수** | 한 Step이 끝나는 즉시 |
  | **티스토리 블로그 초안(.md)** | **프로젝트 공부 세션에서만** | 공부 세션에서 글을 쓸 때 |

  - 작업 세션(프론트엔드/백엔드/아키텍처)은 **개발 로그만 쓰고, 블로그 초안은 쓰지 않는다.**
  - 블로그 초안은 **프로젝트 공부 세션**이 `docs/devlog/`에 쌓인 개발 로그를 재료 삼아 별도로 작성한다.

- **개발 로그 작성 시점 규칙 (모든 작업 세션 공통, 반드시 준수):**
  - 로드맵의 한 Step이나 디버깅 한 건이 끝나면 **그 자리에서 즉시** 개발 로그를 작성한다.
  - **여러 단계를 몰아서 한 번에 쓰지 않는다.** 시간이 지나면 "왜 그렇게 짰는지"를 잊어버려서 기록의 가치가 사라진다.
  - 작성 순서는 항상 **① 동작 확인 → ② 개발 로그 작성 → ③ 커밋 + push** 이다. 커밋 전에 기록을 먼저 남긴다.
  - 사용자가 따로 요청하지 않아도 알아서 작성한다. (요청을 기다리지 않는다)

- **저장 위치 및 파일명 규칙:**
  - 개발 로그와 블로그 초안은 모두 **`docs/devlog/` 폴더에 저장**한다. (git으로 추적되어 데스크톱·노트북 어디서든 이어서 볼 수 있다)
  - 개발 로그: `docs/devlog/<YYYY-MM-DD>_개발로그_<단계번호>단계-<영역>-<주제>.txt`
    - 영역: `백엔드` / `프론트` / `아키텍처`. 로드맵 작업이면 주제 앞에 `Step<번호>-`를 붙인다.
    - 예: `2026-09-12_개발로그_5단계-백엔드-Step1-DB스키마.txt`
  - 블로그 초안: `docs/devlog/<YYYY-MM-DD>_티스토리_블로그초안_<단계번호>단계-<주제>.md`
  - **단계번호 = `docs/devlog/README.md` 목록의 마지막 번호 + 1** (전체 일련번호. 로드맵 Step 번호와 다름)
    - 한 Step에 백엔드·프론트 로그가 따로 생기면 번호도 따로 받는다. (예: 6단계-백엔드-Step2, 7단계-프론트-Step2)
  - 두 파일은 **반드시 분리**해서 만든다. (하나로 합치지 않는다)
  - 파일을 추가하면 `docs/devlog/README.md`의 목록 표에 한 줄 추가한다.
  - `Claude outputs/` 폴더는 `.gitignore`로 막힌 **개인 메모 전용**이다. 개발 로그를 여기에 저장하지 않으며, 이 폴더의 파일은 절대 `git add` 하지 않는다.

- **개발 로그(.txt) 5부 구성:**
  1. 현재 단계 목표
  2. 사용한 기술/핵심 개념 (비유 설명 포함)
  3. 작성/수정된 핵심 코드
  4. 동작 확인 방법
  5. 오늘의 학습 포인트

- **티스토리 블로그 포스팅 초안 작성 규칙 (⚠️ 프로젝트 공부 세션 전용 / 가독성 극대화 및 디자인 최적화):**
  - **여백과 호흡:** 모바일과 PC 어디서든 읽기 편하도록 문단은 **2~3줄 이상 길게 끌지 말고 짧게 끊고, 문단 사이마다 반드시 빈 줄을 넉넉히** 넣어 달라.
  - **도입부 (아이캐처):** 글 상단은독자의 이목을 끌 수 있도록 가벼운 이모지와 함께 **한 줄짜리 핵심 요약 박스(`>`)**로만 깔끔하게 시작한다.
  - **본문 구조화:** 
    - 소제목(`###`) 앞에는 항상 시각적 인식을 돕는 직관적인 이모지(`🎯`, `🛠️`, `💡`, `🔍`)를 붙인다.
    - 줄글 설명보다는 **불릿 포인트(`*`)**와 **볼드체(`**`)**를 적극 활용하여 눈에 정보가 덩어리째 박히도록 구성한다.
  - **디테일한 해설:** "왜 이 코드를 썼는지"에 대한 배경은 딱딱한 설명 대신 **친근한 비유**를 섞어 대화하듯 풀어낸다.
  - **마무리 (핵심 요약):** 글 하단에는 오늘 배운 내용을 한눈에 복습할 수 있는 **'오늘의 핵심 3줄 요약'** 코너를 반드시 넣는다.

## 6. 작업 환경(데스크톱/노트북) 및 Git 운영 규칙
- 이 프로젝트는 **여러 대의 컴퓨터에서 번갈아 작업**한다. 원격 저장소는 `https://github.com/lwk5803/Project-Seoul.git` (branch: `main`).
- **작업 시작 전 반드시 `git pull origin main`** 을 먼저 실행한다. (안 하면 다른 컴퓨터의 작업과 충돌한다)
- **한 Step이 끝나면 `git commit` 후 `git push origin main`** 까지 마무리한다. push하지 않고 자리를 뜨지 않는다.
- 커밋 메시지는 Conventional Commits + 한글 (예: `feat: 캐릭터 생성 API 추가`).
- **절대 커밋하면 안 되는 것:** `.env`(비밀키), `venv/`, `node_modules/`, `Claude outputs/`.
  - 새 컴퓨터에서는 `.env.example`을 복사한 뒤 Supabase 대시보드에서 값을 다시 채운다. 비밀키를 메신저나 메일로 옮기지 않는다.
  - 라이브러리는 옮기지 않고 `pip install -r requirements.txt` / `npm install` 로 각 컴퓨터에서 새로 설치한다.
- 프론트엔드 / 백엔드 / 아키텍처 세션은 **한 번에 하나만** 작업한다. (동시에 커밋하면 충돌)
- git 명령은 **항상 프로젝트 루트(`Project-Seoul/`)에서** 실행한다. (`frontend/`·`backend/` 안에서 치지 않는다)
- 세션에 GitHub 로그인이 안 되어 있거나 세션이 명령을 실행할 수 없으면, **사용자가 cmd에 그대로 붙여넣을 git 명령**(pull / add / commit / push)을 안내한다.
- 커밋 전에 `git status`로 커밋 금지 파일(`.env`, `venv/`, `node_modules/`, `Claude outputs/`)이 섞이지 않았는지 확인한다.

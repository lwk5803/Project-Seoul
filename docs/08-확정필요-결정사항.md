# 08. 결정사항 목록 (확정 / 가정값)

> 📌 **현재 방침**: 아래 **가정값**으로 진행하되, **진행하면서 계속 바꿔갈 수 있는 값**으로 둔다.
> 그래서 설계는 "나중에 바꿔도 덜 아프게" 짜여 있다.
> (예: AI 모델 이름은 환경변수, 칩 문구·태도 표는 `rules.py` 한 곳, 퓨샷은 파일, `memories` 테이블은 Step 10 직전에 생성)
>
> 💡 바꾸고 싶은 항목이 생기면 아키텍처 세션에 "⑦번 40턴을 60턴으로" 처럼 말해주면, 관련 문서를 전부 함께 고친다.

---

## A. 사용자가 확정한 것 (2026-09-11)

| 항목 | 결정 | 반영 문서 |
|---|---|---|
| 게임 기획 | `GAME_CONCEPT.md` — **여백 (Yeobaek)** | 전체 |
| 게임 수치 | 능력치·주사위·HP·돈 **전부 제거**, **감정 상태(mood)만 추적** | 02 4장 |
| 온보딩 입력 | 4요소(대상·감정·상황·장르의 결) — **예시 칩 선택 + 직접 입력** | 02 3장 |
| 플레이 방식 | **태도를 고르는 선택지 2~3개 + 직접 입력** | 02 6장 |
| 결말 | GAME OVER 없음. **플레이어가 스스로 마침표** → 에필로그 | 02 7장 |
| 완성작 | **여백의 서재**에 쌓임 | 02 8장 |
| git | 모든 세션이 **commit + push까지** (못 하면 명령어 안내) | CLAUDE.md 6장 |

---

## B. 가정값 (진행하며 바꿔도 되는 것)

| # | 항목 | 가정값 | 바꾸면 영향 |
|---|---|---|---|
| ① | 플레이 인원 | **1인 플레이** | 여러 명이면 SSE → WebSocket 교체 (큰 공사) |
| ② | AI 제공자 | **미정** — 코드는 모델 이름·키를 환경변수로 받게 짬 | 적음 (Step 10 전까지는 임베딩도 자유) |
| ③ | 로그인 | **이메일 + 비밀번호** | 적음 (소셜 로그인은 나중에 추가해도 기존 코드 안 깨짐) |
| ④ | 서술 시점 | 본문 **1인칭 "나"**, 에필로그 마지막 문단만 **"당신"** | 프롬프트·퓨샷 수정 |
| ⑤ | 서술 길이 | 프롤로그 300~600자 / 본문 200~450자 / 에필로그 400~800자 | 프롬프트 수정 |
| ⑥ | 수위 | **12세 이용가 수준** | ⚠️ 프롬프트·퓨샷·안전 규칙 전부 |
| ⑦ | 턴 상한 | 맺기 가능 **1턴~**, 매듭 유도 **30턴~**, 최대 **40턴** | `rules.py` 숫자만 |
| ⑧ | 태도 표 | 참아내기·피하기·받아치기·털어놓기·흘려보내기 (5종) | `rules.py` + 기록원 프롬프트. ⚠️ 이미 저장된 턴의 태도 코드와 호환 주의 |
| ⑨ | 장르의 결 | 일상/드라마·서늘한 서스펜스·씁쓸한 블랙코미디·잔잔한 위로·풋풋한 설렘 (5종) | 칩 + 퓨샷 파일 추가 |
| ⑩ | 주인공 저장 | **저장하지 않음** — 이야기마다 온보딩에서 새로 정함 | "지난 설정 불러오기"는 나중에 추가 가능 |
| ⑪ | 되돌리기 | 완성 후 되돌리기 **없음**, 서재에서 **지우기는 가능** | 되돌리기를 넣으면 턴 스냅샷 복원 로직 필요 |
| ⑫ | 동시 진행 | 진행 중인 이야기 **여러 편 허용** | 적음 |
| ⑬ | 안전 안내 | 위기 신호 시 조용한 안내 + **자살예방상담전화 109** | 문구만. 배포 전 번호 재확인 |
| ⑭ | 배포 시점 | **Step 11**. 단, Step 5가 끝나면 한 번 시험 배포 권장 | 적음 |

> 💡 **⑦ 턴 상한**은 기획서의 "강제적인 THE END를 두지 않는다"와 부딪히지 않게 설계했습니다.
> 40턴에 닿아도 **자동으로 끝나지 않고**, 더 이어 쓸 수 없을 뿐 마침표는 플레이어가 누릅니다. 비용과 "단편"의 호흡을 위한 책의 두께입니다.

---

## C. 준비물 체크리스트

- [x] Supabase 프로젝트 (`backend/.env`)
- [x] GitHub 원격 저장소 — `https://github.com/lwk5803/Project-Seoul.git` (branch `main`)
- [ ] Supabase 대시보드 → JWT Keys 에서 **비대칭 서명 키** 사용 여부 확인 (Step 0.5 ④, Step 2에 필요)
- [ ] AI API 키 + 결제수단 (②번 결정 후, **Step 5**에 필요)
- [ ] LangSmith 계정 (무료, **Step 5**에 필요)
- [ ] 명조·고딕 글꼴 파일 (OFL 라이선스, **Step 4**에 필요)
- [ ] 퓨샷 문체 예문 — 장르의 결마다 2~3개 (**Step 9**까지. 백엔드 세션이 초안, 사용자가 다듬기)
- [ ] Supabase `pgvector` 확장 활성화 (**Step 10**)
- [ ] Vercel 계정, Render 또는 Railway 계정 (**Step 11**)

---

## 변경 이력
- 2026-09-10: 최초 작성 (아키텍처 세션) — 주사위 TRPG 기준 7개 결정
- 2026-09-11: 현재 방침(가정값으로 진행·수시 변경) 명시, JWT Keys 확인 항목 추가 (아키텍처 세션)
- 2026-09-11: **전면 재작성** (아키텍처 세션) — `GAME_CONCEPT.md`(여백) 반영. 사용자 확정 사항(A)과 가정값(B) 분리. 시나리오·게임 오버 항목 제거, 시점·턴 상한·태도 표·장르의 결·주인공 저장·안전 안내 추가

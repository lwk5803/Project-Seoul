# 개발 로그 / 블로그 초안 보관함

각 작업 단계가 끝날 때마다 쓰는 기록을 모아두는 곳입니다.
**git으로 추적되므로 데스크톱·노트북 어디서 작업하든 그대로 따라옵니다.**

---

## 파일 두 종류

| 파일 | 용도 | 누가 쓰나 |
|---|---|---|
| `<날짜>_개발로그_<단계>.txt` | 내가 나중에 다시 볼 학습/기록용 | **모든 작업 세션 필수** (프론트/백엔드/아키텍처) |
| `<날짜>_티스토리_블로그초안_<단계>.md` | 티스토리에 바로 붙여넣을 글 | **프로젝트 공부 세션에서만** |

> ⚠️ 작업 세션은 **개발 로그만** 씁니다. 블로그 초안은 쓰지 않습니다.
> 블로그 초안은 공부 세션이 여기 쌓인 개발 로그를 재료로 나중에 작성합니다.
> 그래서 개발 로그에 "왜 그렇게 짰는지"를 충분히 적어두는 것이 중요합니다.

### 이름 규칙
```
2026-09-12_개발로그_5단계-백엔드-Step1-DB스키마.txt
└─ 날짜 ──┘ └ 종류 ┘ └번호┘ └영역┘ └─ 주제 ─────┘

2026-09-12_티스토리_블로그초안_5단계-DB스키마.md
```
- 날짜는 `YYYY-MM-DD`
- **단계 번호 = 아래 목록의 마지막 번호 + 1** (프로젝트 전체 일련번호. 로드맵의 Step 번호와 다릅니다)
- 영역: `백엔드` / `프론트` / `아키텍처`. 로드맵 작업이면 주제 앞에 `Step<번호>-`
- 한 Step에 백엔드·프론트 로그가 따로 생기면 **번호도 따로** 받습니다.
- ⚠️ 번호를 정하기 직전에 `git pull` 후 이 목록을 다시 확인합니다. (다른 세션이 방금 번호를 가져갔을 수 있음)
- 블로그 초안의 번호는 재료로 쓴 개발 로그의 번호를 따릅니다. (여러 개를 묶으면 그중 가장 큰 번호)
- 1~3단계 파일은 이 규칙이 생기기 전에 만든 것이라 이름 모양이 조금 다릅니다. (그대로 둠)
- 파일 목록이 날짜순으로 정렬되도록 날짜를 맨 앞에 둡니다.

---

## 왜 `Claude outputs/`가 아니라 여기인가

- `Claude outputs/`는 `.gitignore`에 막혀 있어 **다른 컴퓨터로 따라가지 않습니다.**
- 개발 로그와 블로그 초안에는 비밀키가 없으므로 git에 올려도 안전합니다.
- `Claude outputs/`는 커밋하면 안 되는 **개인 메모 전용**으로 남겨둡니다.

---

## 작성 규칙

작성 형식과 시점은 프로젝트 루트의 [`CLAUDE.md`](../../CLAUDE.md) **5장**에 있습니다.

> 💡 핵심: **한 단계가 끝나면 개발 로그를 그 자리에서 바로 씁니다.** 나중에 몰아서 쓰면
> "왜 그렇게 짰는지"를 이미 잊어버려서 기록의 가치가 사라집니다.

---

## 목록

| 단계 | 날짜 | 영역 | 로드맵 | 내용 |
|---|---|---|---|---|
| 1단계 | 2026-09-10 | 전체 | — | 프로젝트 초기 세팅 (백엔드 FastAPI + 프론트엔드 Next.js) |
| 2단계 | 2026-09-10 | 아키텍처 | Step 0 | 전체 아키텍처 설계 (설계 문서 8종) |
| 3단계 | 2026-09-11 | 백엔드 | — | 깃 병합 충돌 해결 (frontend/README.md 한글 버전 유지) + Supabase API 키 체계 마이그레이션 (legacy → publishable/secret) |
| 4단계 | 2026-09-11 | 아키텍처 | Step 0 | 설계 문서 보완 (문서 간 충돌 정리, API 계약서 빈칸 채움, Step 0.5 신설) |
| 5단계 | 2026-09-11 | 아키텍처 | Step 0 | "여백" 기획 반영 전면 재설계 (감정 상태·태도 선택지·에필로그·서재, UI 가이드 신설) |
